export const theme = {
    colors: {
        background: '#0B0E14',
        primary: '#00FF99', // Neon Green (S-Tier)
        danger: '#FF0055',  // Red (F-Tier)
        secondary: '#00D1FF', // Cyan (A-Tier / Info)
        warning: '#FFD600', // Yellow (B-Tier)
        text: '#FFFFFF',
        textDim: 'rgba(255, 255, 255, 0.6)',
        glass: 'rgba(255, 255, 255, 0.05)',
        glassBorder: 'rgba(255, 255, 255, 0.1)',
    },
    fonts: {
        heading: 'System', // Fallback from Inter-Bold
        body: 'System',   // Fallback from Inter-Regular
        mono: 'Courier',  // Fallback from JetBrainsMono-Regular
    },
    spacing: {
        s: 8,
        m: 16,
        l: 24,
        xl: 32,
    },
    borderRadius: {
        s: 4,
        m: 8,
        l: 16,
    }
};
